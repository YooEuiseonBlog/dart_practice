void main(List<String> args) {
  var giveMeFive = true;
  var numbers = [
    1,
    2, 
    3, 
    4,
    // collection if
    if(giveMeFive) 5,
  ];

  // if(giveMeFive) {
  //   numbers.add(5);
  // }

  // List<int> numbers = [1, 2, 3, 4];
  // numbers.add('1'); // type이 맞아야 한다.
  // numbers.add(1);

  print(numbers);

} 